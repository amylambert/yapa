from django.apps import AppConfig


class ProjectsConfig(AppConfig):
    """Configuration mapping for the updated projects subsystem."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "projects"